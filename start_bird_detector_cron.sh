#!/bin/bash
# Обертка для Cron + Screen автозапуска Bird Detector
# Вызывает основной скрипт start_bird_detector_cron_screen.sh

# Путь к основному скрипту
MAIN_SCRIPT="$(dirname "$0")/start_bird_detector_cron_screen.sh"

# Проверка наличия основного скрипта
if [ ! -x "$MAIN_SCRIPT" ]; then
    echo "❌ Ошибка: Основной скрипт не найден или не исполняемый: $MAIN_SCRIPT"
    exit 1
fi

# Запуск основного скрипта
echo "🔄 Перенаправление на основной скрипт: $MAIN_SCRIPT"
exec "$MAIN_SCRIPT" "$@"
