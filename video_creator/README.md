# Video Creator v1.0

Инструмент для создания видеоряда (таймлапс) из фотографий Bird Detector.

## 🚀 Быстрый старт

### Автоматическая настройка

```bash
# Сделать скрипт исполняемым
chmod +x setup_venv.sh

# Запустить автоматическую настройку
./setup_venv.sh
```

### Ручная настройка

```bash
# 1. Установить системные зависимости
sudo apt update
sudo apt install ffmpeg

# 2. Создать виртуальное окружение
python3 -m venv venv

# 3. Активировать и установить зависимости
source venv/bin/activate
pip install -r requirements.txt

# 4. Создать необходимые папки
mkdir -p videos logs
```

## 🎬 Использование

### Базовое использование

```bash
# Активировать виртуальное окружение
source venv/bin/activate

# Создать видео с конфигом по умолчанию
python video_creator.py

# Использовать конкретный конфиг
python video_creator.py --config my_config.yaml
```

### Параметры командной строки

- `--config, -c`: Путь к конфигурационному файлу (по умолчанию: `video_creator_config.yaml`)
- `--dry-run`: Только анализ кадров, без создания видео
- `--verbose, -v`: Подробный вывод
- `--help`: Справка
- `--version`: Версия

### Примеры

```bash
# Тестовый запуск
python video_creator.py --dry-run

# Создание видео с пользовательским конфигом
python video_creator.py --config custom_config.yaml

# Подробный вывод
python video_creator.py --verbose
```

## ⚙️ Конфигурация

Основной файл конфигурации: `video_creator_config.yaml`

### Пример конфигурации для Bird Detector

```yaml
input:
  frames_folder: "../bird_logs/logs_20251214_154000/photos"
  tag: "count"
  start_frame: 1
  end_frame: 50
  exclude_frames: []

timing:
  interval_mode: "fixed"
  frame_interval_ms: 500

output:
  video_filename: "bird_timelapse.mp4"
  output_folder: "./videos"
```

### Конфигурация с аудио

```yaml
audio:
  enable_audio: true
  audio_file: "/path/to/nature_sounds.mp3"
  volume: 0.3
```

## 📁 Структура проекта

```
21_video_creator/
├── video_creator.py              # Основной скрипт
├── video_creator_config.yaml     # Конфигурационный файл
├── video_creator_config_example.yaml  # Пример конфигурации
├── requirements.txt              # Зависимости Python
├── setup_venv.sh                 # Скрипт настройки
├── README.md                     # Эта документация
├── venv/                         # Виртуальное окружение (создается)
├── videos/                       # Выходные видео (создается)
└── logs/                         # Логи (создается)
```

## 🔧 Устранение неисправностей

### Проблема: FFmpeg не найден

```bash
sudo apt install ffmpeg
ffmpeg -version
```

### Проблема: Нет кадров

```bash
# Проверить путь к папке с фото
ls -la /path/to/bird_logs/latest/photos/

# Проверить конфиг
cat video_creator_config.yaml | grep frames_folder
```

### Проблема: Ошибка импорта

```bash
# Активировать виртуальное окружение
source venv/bin/activate

# Переустановить зависимости
pip install -r requirements.txt
```

## 📊 Логи

Логи записываются в:
- `video_creation.log` - основной лог
- Консоль (если включено в конфиге)

## 🔗 Интеграция с Bird Detector

Для автоматического создания видео из последних фотографий:

```bash
# Найти последнюю папку с логами
LATEST_LOG=$(ls -td ../bird_logs/logs_*/ | head -1)

# Обновить конфиг
sed -i "s|frames_folder:.*|frames_folder: $LATEST_LOG/photos|" video_creator_config.yaml

# Создать видео
python video_creator.py
```

## 📋 Требования

- **Python**: 3.11+
- **FFmpeg**: Любая версия
- **Операционная система**: Linux

---

**Создано для Bird Detector v5.5** 🐦📹
