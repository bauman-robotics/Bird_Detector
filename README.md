# Bird Detector v5.5 - Система детекции и подсчета птиц

[![Version](https://img.shields.io/badge/version-5.5-blue.svg)](https://github.com/bauman-robotics/Bird_Detector)
[![Python](https://img.shields.io/badge/python-3.11+-green.svg)](https://www.python.org/)
[![Raspberry Pi](https://img.shields.io/badge/platform-Raspberry%20Pi-red.svg)](https://www.raspberrypi.org/)
[![Hailo](https://img.shields.io/badge/AI-Hailo%208-purple.svg)](https://hailo.ai/)

## 📋 Описание проекта

**Bird Detector** - интеллектуальная система детекции и подсчета птиц на кормушке с использованием нейронной сети Hailo-8. Система разработана для Raspberry Pi и обеспечивает стабильную работу с производительностью 24-32 FPS.

### 🎯 Основные возможности

- **🧠 AI детекция птиц** с использованием Hailo-8 акселератора
- **📊 Умный подсчет** уникальных птиц и посещений кормушки
- **🌐 Веб-стримы** камеры и детекции в реальном времени (с **температурой CPU**)
- **📸 Автосохранение фото** при обнаружении птиц
- **📝 Многоуровневое логирование** с мониторингом температуры
- **🔄 Автозапуск** через Cron + Screen
- **🔧 Полная диагностика** окружения и производительности

## 🚀 Быстрый старт

### Требования к системе

- **Raspberry Pi 5** с Raspberry Pi OS
- **Hailo-8 AI акселератор** (Hailo-8)
- **Камера** Raspberry Pi Camera 
- **Python 3.11+** с установленным виртуальным окружением

### Установка и запуск

```bash
# 1. Клонировать репозиторий
git clone https://github.com:bauman-robotics/Bird_Detector.git

# 2. Настроить виртуальное окружение (если не настроено)
# Следовать инструкциям из 15_dev/ директории

# 3. Запустить Bird Detector
./start_bird_detector_cron_screen.sh

# 4. Проверить работу
screen -list  # Должна быть сессия bird_detector
curl http://localhost:8080  # Камера стрим
curl http://localhost:8091  # Детекция стрим
```

### Автозапуск при загрузке

```bash
# Настроить cron для автозапуска
crontab -e
# Добавить строку:
@reboot sleep 30 && /home/pi/projects/Hailo8_projects/Bird_Detector/start_bird_detector_cron.sh
```

## ⚙️ Конфигурация

### Основной файл конфигурации: `bird_counter_config_v5.yaml`

```yaml
# ==============================================================================
# Bird Detector v5.6 - Конфигурационный файл
# ==============================================================================

# Умный подсчет птиц (из v2, улучшенный в v5.3)
bird_tracking:
  enable_tracking: true           # Включить трекинг уникальных птиц
  bird_timeout_seconds: 30        # Время жизни птицы без детекции (сек)
  max_simultaneous_birds: 10      # Максимальное количество одновременных птиц
  enable_visit_counter: true      # Включить счетчик посещений кормушки
  min_time_between_visits_seconds: 10  # Минимальное время между посещениями (сек)

# Логирование
logging:
  enable_text_log: true           # Включить текстовый лог детекций
  log_format: "markdown"          # Формат лога: markdown, json, csv
  log_filename_pattern: "bird_log_{timestamp}.md"  # Шаблон имени файла с timestamp
  logs_path: "./bird_logs"        # Базовый путь для логов
  console_output_mode: "minimal"  # Режим консольного вывода: "all", "changes_only", "minimal"

# Сохранение кадров
frame_saving:
  enable_photo_save: true         # Сохранять фото при обнаружении птиц
  min_save_interval_seconds: 5    # Минимальный интервал между сохранениями (сек)
  photo_filename_pattern: "bird_{timestamp}_count{bird_count}.jpg"
  frames_subfolder: "photos"      # Подпапка для фото

# Детекция
detection:
  target_classes: ["bird"]        # Целевые классы для детекции
  min_confidence: 0.3             # Минимальная уверенность детекции (0.0-1.0)
  min_bbox_size: 0.01             # Минимальный размер bounding box
  max_bbox_size: 0.8              # Максимальный размер bounding box

# Параметры модели Hailo
hailo_model:
  hef_path: "/usr/share/hailo-models/yolov8s_h8l.hef"  # Путь к HEF файлу
  # Доступные модели для Hailo-8:
  # - yolov8s_h8l.hef (баланс производительности ~30 FPS)
  # - yolov8m_h8l.hef (высокая точность, ~20 FPS)
  # - yolov5s_h8l.hef (максимальная скорость ~40 FPS)

# Веб-стримы
web_streams:
  stream_mode: "both"             # Режим стрима: "camera_only", "detection_only", "both"
  enable_camera_stream: true      # Чистый стрим камеры
  enable_detection_stream: true   # Стрим с детекцией
  camera_stream_port: 8080        # Порт для чистого стрима
  detection_stream_port: 8091     # Порт для стрима с детекцией
  stream_quality: 80              # Качество JPEG (1-100)

# Общие настройки
general:
  enable_console_output: true     # Выводить информацию в консоль
  debug_mode: false               # Режим отладки
  test_mode: false                # Тестовый режим

# Статистика и метрики
statistics:
  enable_fps_display: true        # Показывать FPS
  enable_bird_count_display: true # Показывать счетчики птиц
  update_interval_seconds: 1      # Интервал обновления статистики

# Мониторинг системы
system_monitoring:
  enable_temperature_logging: true    # Включить логирование температуры
  temperature_log_interval_minutes: 0.1  # Интервал логирования температуры
  temperature_log_filename: "system_temperature_{timestamp}.log"

# Отладка производительности
performance_debug:
  enable_performance_log: false       # Включить детальный лог производительности
  performance_log_filename: "performance_debug_{timestamp}.log"

# Диагностика запуска
startup_diagnostics:
  enable_startup_log: true            # Включить лог диагностики запуска
  startup_log_filename: "startup_diagnostics_{timestamp}.log"
```

## 🎮 Использование

### Ручной запуск

```bash
# Основной запуск с screen
./start_bird_detector_cron_screen.sh

# Или напрямую (для тестирования)
source /home/pi/projects/Hailo8_projects/Bird_Detector/venv/bin/activate
python3 bird_detector_v5_5.py --input rpi
```

### Управление screen сессиями

```bash
# Просмотр активных сессий
screen -list

# Подключение к сессии для отладки
screen -r bird_detector

# Внутри сессии:
# Ctrl+A, D - отключиться (сессия продолжает работать)
# Ctrl+A, K - завершить сессию

# Завершение сессии извне
screen -S bird_detector -X quit
```

### Веб-интерфейс

После запуска доступны два веб-стрима:

- **Camera Stream**: http://localhost:8080
  - Чистый поток с камеры без обработки

- **Detection Stream**: http://localhost:8091
  - Поток с наложенной детекцией птиц
  - Bounding boxes, счетчики, FPS, **температура процессора**

### Логи и мониторинг

```bash
# Просмотр структуры логов
tree ./bird_logs/
# logs_20251214_154000/
# ├── bird_log_20251214_154000.md          # Основной лог
# ├── system_temperature_20251214_154000.log # Температура + FPS
# ├── startup_diagnostics_20251214_154000.log # Диагностика запуска
# ├── bird_counter_events_20251214_154000.md  # События счетчиков
# ├── add_logs/
# │   ├── bird_counter_log.md               # Лог v2.0
# │   └── performance_debug_20251214_154000.log # Отладка FPS
# └── photos/                               # Сохраненные фото
#     └── bird_20251214_154952_count1.jpg

# Мониторинг температуры и FPS
tail -f ./bird_logs/*/system_temperature_*.log

# Включение отладочного лога производительности
# В bird_counter_config_v5.yaml установить:
performance_debug:
  enable_performance_log: true
```

## 🏗️ Архитектура системы

### Основные компоненты

```
Bird Detector v5.6
├── 🧠 BirdDetectorV55      # Основной класс детектора
├── 🐦 BirdTracker          # Трекер уникальных птиц
├── 📝 LogManager           # Система логирования
├── ⚙️ ConfigManager        # Менеджер конфигурации
├── 🌐 Web Servers          # HTTP серверы для стримов
└── 🔧 Utilities            # Вспомогательные функции
```

### Поток данных

```
Camera → GStreamer → Hailo AI → Detection → Tracking → Logging → Web Streams
```

### Режимы работы

1. **camera_only** - только стрим камеры (порт 8080)
2. **detection_only** - только стрим с детекцией (порт 8091)
3. **both** - оба стрима одновременно

## 🔧 Расширенная конфигурация

### Настройка производительности

```yaml
# Для максимальной производительности
detection:
  min_confidence: 0.4      # Повысить уверенность
  min_bbox_size: 0.05      # Игнорировать мелкие объекты

hailo_model:
  hef_path: "yolov5s_h8l.hef"  # Быстрая модель
```

### Настройка логирования

```yaml
# Для детального логирования
logging:
  console_output_mode: "all"  # Полный вывод в консоль

performance_debug:
  enable_performance_log: true  # Детальный лог FPS
```

### Настройка автозапуска

```bash
# Cron с задержкой для стабильности системы
@reboot sleep 60 && /home/pi/projects/Hailo8_projects/Bird_Detector/start_bird_detector_cron.sh

# Или для определенного времени
@reboot sleep 30 && /home/pi/projects/Hailo8_projects/Bird_Detector/start_bird_detector_cron.sh
0 6 * * * /home/pi/projects/Hailo8_projects/Bird_Detector/start_bird_detector_cron.sh  # Ежедневно в 6:00
```

## 📊 Мониторинг и диагностика

### Системные метрики

```bash
# Температура процессора
vcgencmd measure_temp

# Использование CPU/GPU
top -p $(pgrep -f bird_detector)

# Сетевые подключения
netstat -tlnp | grep :808
```

### Логи системы

```bash
# Systemd логи (если используется)
sudo journalctl -u bird-detector -f

# Cron логи
grep CRON /var/log/syslog

# Screen логи
tail -f /home/pi/bird_detector_cron.log
```

### Диагностика проблем

```bash
# Проверить окружение запуска
cat ./bird_logs/*/add_logs/startup_diagnostics_*.log

# Проверить производительность
tail -20 ./bird_logs/*/system_temperature_*.log

# Проверить работу стримов
curl -I http://localhost:8080
curl -I http://localhost:8091
```

## 🐛 Устранение неисправностей

### Проблема: Низкий FPS или задержки

```bash
# 1. Проверить окружение запуска
cat ./bird_logs/*/add_logs/startup_diagnostics_*.log

# 2. Сравнить с console запуском
DISPLAY=:0 python3 bird_detector_v5_5.py --input rpi

# 3. Проверить переменные окружения в screen сессии
screen -r bird_detector
env | grep -E "(DISPLAY|XAUTHORITY|GST_PLUGIN)"
```

### Проблема: Не работает автозапуск

```bash
# Проверить cron
crontab -l

# Проверить логи cron
grep CRON /var/log/syslog | tail -10

# Ручной тест скрипта
./start_bird_detector_cron_screen.sh
```

### Проблема: Не сохраняются фото

```yaml
# Проверить настройки в конфиге
frame_saving:
  enable_photo_save: true
  min_save_interval_seconds: 5
```

```bash
# Проверить права на директорию
ls -la ./bird_logs/
ls -la ./bird_logs/*/photos/
```

## 🔄 Обновление системы

### Обновление из репозитория

```bash
cd /path/to/project
git pull origin main

# Перезапустить с новой версией
screen -S bird_detector -X quit
./start_bird_detector_cron_screen.sh
```

### Обновление зависимостей

```bash
# Активировать виртуальное окружение
source /home/pi/projects/Hailo8_projects/Bird_Detector/venv/bin/activate

# Обновить пакеты
pip install -r requirements.txt --upgrade

# Перезапустить
./start_bird_detector_cron_screen.sh
```

## 📚 API и интеграция

### HTTP API (будущие версии)

```python
# Получение статистики
GET /api/stats
# {"unique_birds": 5, "visits": 12, "fps": 28.5, "temperature": 65.2}

# Получение конфигурации
GET /api/config

# Управление детекцией
POST /api/control {"action": "restart"}
```

### Webhook уведомления (план)

```yaml
# В конфиге
notifications:
  enable_webhooks: true
  webhook_url: "https://your-server.com/webhook"
  events: ["bird_detected", "visit_count_changed"]
```

## 🤝 Вклад в проект

### Для разработчиков

1. **Форк репозитория** и создание feature branch
2. **Тестирование изменений** в разных окружениях
3. **Документирование** новых функций
4. **Pull request** с подробным описанием

### Для пользователей

- **Сообщения об ошибках** через Issues
- **Предложения улучшений** приветствуются
- **Документация проблем** помогает улучшить систему


## 🙏 Благодарности

- **Hailo AI** 
- **Raspberry Pi Foundation** за платформу
- **Сообщество Open Source** за инструменты и библиотеки

## 📞 Поддержка

- **Документация**: [Полная документация](docs/)
- **Issues**: [GitHub Issues](https://github.com/bauman-robotics/Bird_Detector-8/issues)
- **Wiki**: [Проектная Wiki](https://github.com/bauman-robotics/Bird_Detector/wiki)

---

**Bird Detector v5.5** - интеллектуальная система детекции птиц с максимальной производительностью и надежностью! 🐦✨
